/**
 * author: Habibie Ed Dien
 * purpose: for tracking code
 */

export function tracking(i: number, my: string, name_file?: string) { //optional parameter `name_file`
    name_file = name_file || '###'; // if `name_file` not set, than equal `###`
    //console.log("langkah " + i + " : " + my + " - " + name_file);
}
